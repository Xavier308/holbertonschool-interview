#ifndef SORT_H
#define SORT_H

#include <stddef.h>

/* Provided print_array prototype */
void print_array(const int *array, size_t size);

/* Radix sort prototype */
void radix_sort(int *array, size_t size);

#endif /* SORT_H */
